{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Supabase": {
    "Authority": ""
  },
  "Email": {
    // Required when replacing ConsoleEmailSender with a real provider in Program.cs.
    // Add provider-specific keys here (e.g. "ApiKey", "FromAddress", "FromName").
    // Map env vars via the AddInMemoryCollection block in Program.cs.
  },
  "Scheduling": {
    "MaxRescheduleCount": 3
  }
}
